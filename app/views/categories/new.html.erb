<% if is_admin? %>
  <h1>New Category</h1>

  <%= render 'form' %>
<% else %>
  You are not admin. You do not have adding, editing or deleting permission.
<% end %>

  <%= link_to 'Back', categories_path %>
