<!-- create a new category only allowed by admin -->

<% if is_admin? %>
  <h1>New Category</h1>

<!-- renders partial form to create a new brand -->
  <%= render 'form' %>
<% else %>
  You are not admin. You do not have adding, editing or deleting permission.
<% end %>

  <%= link_to 'Back', categories_path %>
