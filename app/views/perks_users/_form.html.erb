<%= form_for(@perks_user) do |f| %>
  <% if @perks_user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@perks_user.errors.count, "error") %> prohibited this perks_user from being saved:</h2>

      <ul>
      <% @perks_user.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
