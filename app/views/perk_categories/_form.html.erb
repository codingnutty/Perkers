<%= form_for(@perk_category) do |f| %>
  <% if @perk_category.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@perk_category.errors.count, "error") %> prohibited this perk_category from being saved:</h2>

      <ul>
      <% @perk_category.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
