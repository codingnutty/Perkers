<article class="all-perks">
<h1>Listing Perks near you</h1>
    <% @perks.each do |perk| %>
        <% if @user.zipcode == perk.zipcode %>

          <div  class="perks-list">
            <div class="panel panel-primary" id="loc-perk<%=perk.id %>"
              <div class='panel-body'>
                <a href="/perks/<%= perk.id%>">
                  <%= perk.title %>
                </a>
              </div>

                  <%= perk.discount %> %
                  <div class='panel' style='border: none;'>
                    <% if @errors %>
                    <h3><%= @errors %></h3>
                    <% end %>
                  </div>
                 <div class='panel-footer'>
                     <% if current_user %>
                      <%= render partial: '/favourites/favourite_button', locals: {perk: perk} %>
                      <%= render partial: '/favourites/unfavourite_button', locals: {perk: perk } %>
                     <% end %>
                     <% if is_admin? %>
                        <%= link_to 'Edit', edit_perk_path(perk) %>
                        <%= link_to 'Destroy', perk, method: :delete, data: { confirm: 'Are you sure?' } %>
                     <% end %>
                 </div>
             </div>
          </div>
          <% end %>
    <% end %>
 <h1>Listing all Perks</h1>
    <% @perks.each do |perk| %>
        <% if @user.zipcode != perk.zipcode %>
            <div class="perks-list">
              <div class="panel panel-primary" id="all-perk<%=perk.id%>"
                <div class='panel-body'>
                  <a href="/perks/<%= perk.id%>">
                    <%= perk.title %>
                  </a>
                </div>
                    <%= perk.discount %> % </td>
                    <div class='panel' style='border: none;'>
                      <% if @errors %>
                        <h3><%= @errors %></h3>
                      <% end %>
                    </div>
                <div class='panel-footer'>
                     <% if current_user %>
                      <%= render partial: '/favourites/favourite_button', locals: {perk: perk} %>
                      <%= render partial: '/favourites/unfavourite_button', locals: {perk: perk}%>

                     <% end %>

                    <% if is_admin? %>
                        <%= link_to 'Edit', edit_perk_path(perk) %>
                        <%= link_to 'Destroy', perk, method: :delete, data: { confirm: 'Are you sure?' } %>
                    <% end %>
                </div>
              </div>
            </div>
        <% end %>
    <% end %>
<% if is_admin? %>
  <br>
  <br>
  <button class ="btn-lg">
    <%= link_to 'Add Perk', new_perk_path(@perks) %>
  </button>
<% end %>
</article>